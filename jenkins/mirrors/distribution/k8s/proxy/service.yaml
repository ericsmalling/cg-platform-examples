apiVersion: v1
kind: Service
metadata:
  name: distribution-proxy
  namespace: mirror-distribution
  labels:
    app: distribution-proxy
spec:
  type: NodePort
  selector:
    app: distribution-proxy
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30050
      protocol: TCP
