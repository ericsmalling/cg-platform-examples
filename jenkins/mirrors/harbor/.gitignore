# Terraform local state and provider plugin cache (stage-2 Harbor module).
# Stage-1 (chainguard_identity) state lives in mirrors/_common/terraform/.
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan

# Rendered manifests / values (regenerated by deploy.sh from .template files).
cg/manifests/deploy-ingress-nginx.yaml
cg/helm/values.yaml
